---
phase: 03-veena-company-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/crewai/agents/veena/SOUL.md
  - platform/crewai/agents/veena/mcp.json
  - platform/crewai/agents/veena/skills/00-product-marketing-context.md
  - platform/crewai/agents/veena/skills/01-company-crawl.md
  - platform/crewai/agents/veena/memory/MEMORY.md
  - platform/content-engine/backend-server.js
autonomous: true
requirements:
  - VEENA-01

must_haves:
  truths:
    - "platform/crewai/agents/veena/ directory exists with all required files (SOUL.md, mcp.json, skills/, memory/MEMORY.md)"
    - "Veena's SOUL.md declares reads_from_mkg, writes_to_mkg, and triggers_agents fields — establishing the pattern Phase 4 requires for all agents"
    - "POST /api/agents/veena/run returns an agent response (not 404) — veena is registered in VALID_AGENTS and AGENT_PROFILES"
    - "Veena's skills/ contains 00-product-marketing-context.md (loaded first) and 01-company-crawl.md (crawl extraction guide)"
  artifacts:
    - path: "platform/crewai/agents/veena/SOUL.md"
      provides: "Veena identity, MKG ownership declaration, mission, rules"
      contains: "writes_to_mkg"
    - path: "platform/crewai/agents/veena/mcp.json"
      provides: "Connector registration (empty at Phase 3)"
      contains: "connectors"
    - path: "platform/crewai/agents/veena/skills/00-product-marketing-context.md"
      provides: "MKG schema awareness — loaded first on every run"
    - path: "platform/crewai/agents/veena/skills/01-company-crawl.md"
      provides: "Crawl extraction methodology guide for the 12 MKG fields"
    - path: "platform/crewai/agents/veena/memory/MEMORY.md"
      provides: "Empty initialized memory file"
    - path: "platform/content-engine/backend-server.js"
      provides: "veena added to VALID_AGENTS set and AGENT_PROFILES object"
      contains: "veena"
  key_links:
    - from: "platform/content-engine/backend-server.js"
      to: "platform/crewai/agents/veena/"
      via: "VALID_AGENTS set + fs.readFile(agentDir/SOUL.md)"
      pattern: "VALID_AGENTS.*veena|veena.*VALID_AGENTS"
---

<objective>
Create the Veena agent file structure and register her with the backend. No crawl logic in this plan — just the identity files and VALID_AGENTS registration that every other plan depends on.

Purpose: Veena cannot be invoked via the run endpoint, and the crawler cannot be written correctly, until her SOUL.md and VALID_AGENTS entry exist. This is the prerequisite for Plans 03-02 and 03-03.
Output: Five new files in platform/crewai/agents/veena/ and two additions to backend-server.js (VALID_AGENTS + AGENT_PROFILES).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-veena-company-intelligence/03-RESEARCH.md

# Reference agent for file format — zara is the canonical example
@platform/crewai/agents/zara/SOUL.md
@platform/crewai/agents/zara/mcp.json
@platform/crewai/agents/zara/memory/MEMORY.md
@platform/crewai/agents/isha/skills/00-product-marketing-context.md
</context>

<interfaces>
<!-- Key patterns extracted from existing agents. Use these exactly — do not invent new formats. -->

From platform/crewai/agents/zara/SOUL.md structure:
```markdown
# AgentName — Role Title

**Role**: ...
**Personality**: ...
**Expertise**: ...

**Schedule**: ...
**Memory**: agents/{name}/memory/MEMORY.md

## My Mission
...

## What I Produce Each Run
...

## My Rules
...
```

From backend-server.js line 61 (VALID_AGENTS):
```javascript
const VALID_AGENTS = new Set([
  "zara", "maya", "riya", "arjun", "dev", "priya", "tara", "neel", "isha",
  // Add "veena" here
]);
```

AGENT_PROFILES object (starts around line 72) — each entry has:
```javascript
"agentname": {
  name: "Display Name",
  role: "Role description",
  // ... other fields matching existing entries
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Veena agent directory and files</name>
  <files>
    platform/crewai/agents/veena/SOUL.md
    platform/crewai/agents/veena/mcp.json
    platform/crewai/agents/veena/skills/00-product-marketing-context.md
    platform/crewai/agents/veena/skills/01-company-crawl.md
    platform/crewai/agents/veena/memory/MEMORY.md
  </files>
  <action>
Create the veena agent directory with all five required files. Match the exact format of existing agents (zara, isha) — do not deviate from their conventions.

**platform/crewai/agents/veena/SOUL.md**

Write the following content exactly:

```markdown
# Veena — Company Intelligence Agent

**Role**: MKG owner — crawls company websites, bootstraps the Marketing Knowledge
          Graph, and triggers the sequential onboarding chain for new companies
**Personality**: Methodical, evidence-first — reports only what the website confirms;
                 flags all assumptions explicitly
**Expertise**: Company profiling, offer extraction, ICP inference, messaging analysis

**reads_from_mkg**: [] (Veena is the bootstrap agent — reads nothing, writes everything)
**writes_to_mkg**: positioning, icp, competitors, offers, messaging, channels, funnel,
                   metrics, baselines, content_pillars, campaigns, insights
**triggers_agents**: isha, neel, zara (sequential onboarding chain)

**Schedule**: Weekly Mon 06:00 IST (APScheduler — Phase 4)
**Memory**: agents/veena/memory/MEMORY.md

## My Mission
I am the first agent to run for any new company. I crawl the company website and
populate all 12 MKG fields to give every other agent a knowledge foundation to
build on. Without me, every other agent starts cold.

## What I Produce Each Run
- A context_patch covering all 12 MKG top-level fields (value + confidence per field)
- handoff_notes summarizing what was found vs what requires deeper research
- tasks_created entries for isha, neel, zara (sequential onboarding chain)

## My Rules
- Never invent data the website does not support — use confidence 0.3 for inferred fields
- For fields the website makes explicit, use confidence 0.7–0.85
- Always populate all 12 fields — use { value: null, confidence: 0 } if genuinely absent
- Summarize each field as a structured object, not raw prose
- Only visit URLs that are subdomains or paths of the target company domain — never external sites
- If crawl fails or times out, save partial results; do not return an error that blocks the chain
```

**platform/crewai/agents/veena/mcp.json**

```json
{
  "connectors": [],
  "permissions": "read",
  "description": "Veena crawls company websites to bootstrap the MKG. GA4 and Composio connectors will be added in Phase 4.",
  "recipes": {}
}
```

**platform/crewai/agents/veena/skills/00-product-marketing-context.md**

Copy the content from platform/crewai/agents/isha/skills/00-product-marketing-context.md verbatim. The 00- prefix guarantees it loads first via lexicographic sort.

**platform/crewai/agents/veena/skills/01-company-crawl.md**

Write the following content exactly:

```markdown
# Company Crawl Skill

When asked to crawl a company website, follow this process in order:

1. Visit the homepage — read: headline, tagline, navigation items, CTA buttons, hero copy
2. Visit the /pricing or /plans page if it exists — extract offer names, price signals, tiers
3. Visit the /about or /about-us page — extract company size signals, founding story, ICP clues
4. Infer positioning from: headline, tagline, differentiators stated in copy
5. Infer ICP from: case study industries, testimonial job titles, "designed for X" copy
6. Infer channels from: social media links, nav items labelled "blog", "webinar", "podcast"

## Confidence Calibration

| Level | Range | When to use |
|-------|-------|-------------|
| Explicit | 0.8–1.0 | Field stated directly on the website |
| Inferable | 0.5–0.79 | Field clearly inferable from multiple consistent signals |
| Weak inference | 0.3–0.49 | Field weakly inferable from limited or ambiguous signals |
| Not found | 0 | Field not findable from the website; set value: null |

## Output Format

For each MKG field, return exactly:
```json
{
  "value": <structured object, array, or null>,
  "confidence": <number 0.0–1.0>
}
```

Never return raw prose as a value. Always use structured objects.
All 12 fields must be present in your output. Use { "value": null, "confidence": 0 }
for fields not findable on the website.

## The 12 MKG Fields

- positioning: { statement: string, unique_value: string }
- icp: { company_size: string, industry: string, geography: string[], role: string }
- competitors: [{ name: string, positioning: string }]
- offers: [{ name: string, price_signal: string, tier: string }]
- messaging: { headline: string, tagline: string, key_messages: string[] }
- channels: [{ channel: string, evidence: string }]
- funnel: { entry_points: string[], cta_primary: string }
- metrics: null on first crawl (website rarely reveals KPIs)
- baselines: null on first crawl (no baseline established yet)
- content_pillars: [{ topic: string, evidence: string }]
- campaigns: null on first crawl (live campaigns not visible from website)
- insights: { summary: string, gaps: string[] }
```

**platform/crewai/agents/veena/memory/MEMORY.md**

```markdown
# Veena Memory

_No runs yet._
```
  </action>
  <verify>
    <automated>ls /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/SOUL.md /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/mcp.json /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/skills/00-product-marketing-context.md /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/skills/01-company-crawl.md /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/memory/MEMORY.md && grep -l "writes_to_mkg" /Users/yogs87/Documents/New\ project/marqq/platform/crewai/agents/veena/SOUL.md</automated>
  </verify>
  <done>
All five files exist. SOUL.md contains writes_to_mkg and triggers_agents fields. mcp.json is valid JSON. 00-product-marketing-context.md content matches isha's. 01-company-crawl.md includes the confidence calibration table and all 12 MKG field definitions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register veena in VALID_AGENTS and AGENT_PROFILES</name>
  <files>platform/content-engine/backend-server.js</files>
  <action>
Open platform/content-engine/backend-server.js and make exactly two additions. Do not touch any other code.

**Addition 1 — VALID_AGENTS (around line 61):**
Find the `const VALID_AGENTS = new Set([` declaration. Add `"veena"` as the first entry in the set. The research confirms this is the gate for the run endpoint — veena must be here before the onboard endpoint (Plan 03-03) can call agent logic.

```javascript
const VALID_AGENTS = new Set([
  "veena",   // Company Intelligence — Phase 3
  "zara",
  "maya",
  "riya",
  "arjun",
  "dev",
  "priya",
  "tara",
  "neel",
  "isha",
  // kiran and sam may already be present — leave as-is
]);
```

**Addition 2 — AGENT_PROFILES (around line 72 onward):**
Find the `AGENT_PROFILES` object or equivalent constant that maps agent names to profile metadata (name, role, etc.). Add a veena entry matching the exact shape of existing entries. Look at the actual shape of existing entries (e.g., zara, maya) and mirror it. The fields must match what `/api/agents/context` returns. Minimal required fields:

```javascript
veena: {
  name: "Veena",
  role: "Company Intelligence",
  description: "Crawls company websites to bootstrap the Marketing Knowledge Graph with all 12 fields populated before any other agent runs.",
  schedule: "Weekly Mon 06:00 IST",
  writes_to_mkg: ["positioning", "icp", "competitors", "offers", "messaging", "channels", "funnel", "metrics", "baselines", "content_pillars", "campaigns", "insights"],
},
```

After making both changes, confirm the file has no syntax errors by checking it can be parsed — the server must start clean.
  </action>
  <verify>
    <automated>grep -n "veena" /Users/yogs87/Documents/New\ project/marqq/platform/content-engine/backend-server.js | head -20</automated>
  </verify>
  <done>
"veena" appears in both VALID_AGENTS and AGENT_PROFILES. Backend starts without syntax errors (node --check platform/content-engine/backend-server.js passes). POST /api/agents/veena/run returns something other than 404 (agent found, even if run fails due to missing SOUL.md content being wrong).
  </done>
</task>

</tasks>

<verification>
1. `ls platform/crewai/agents/veena/` shows: SOUL.md, mcp.json, skills/, memory/
2. `ls platform/crewai/agents/veena/skills/` shows: 00-product-marketing-context.md, 01-company-crawl.md
3. `grep "writes_to_mkg" platform/crewai/agents/veena/SOUL.md` returns the field line
4. `grep -c "veena" platform/content-engine/backend-server.js` returns >= 2 (VALID_AGENTS + AGENT_PROFILES)
5. `node --check platform/content-engine/backend-server.js` exits with code 0
</verification>

<success_criteria>
VEENA-01 satisfied: Veena agent directory exists at platform/crewai/agents/veena/ with all required files. Backend recognizes "veena" as a valid agent. The SOUL.md declares the MKG ownership contract (reads_from_mkg, writes_to_mkg, triggers_agents) that all Phase 4 agents will mirror.
</success_criteria>

<output>
After completion, create `.planning/phases/03-veena-company-intelligence/03-01-SUMMARY.md` with:
- List of files created (with absolute paths)
- Confirmation that SOUL.md includes reads_from_mkg, writes_to_mkg, triggers_agents fields
- Confirmation that veena appears in VALID_AGENTS and AGENT_PROFILES
- Any deviations from the plan (e.g., AGENT_PROFILES had a different shape than expected)
- Node syntax check result

Then update STATE.md:
- Set Current Position to Phase 3, Plan 1 complete
</output>
