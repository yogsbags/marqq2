import { SocialMediaFlow } from './SocialMediaFlow';

export function SEOLLMOFlow() {
  // Use the Social Media Flow component (100% as-is clone from projects/social-media)
  // This is mapped to the "AI Content" menu item
  return <SocialMediaFlow />;
}
