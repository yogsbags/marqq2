# App auth entry (login/signup). Used for all "Log in" links (data-auth).
VITE_MARQQ_APP_URL=https://marqq.up.railway.app

# Public site origin for canonical / Open Graph in HTML: update index.html
# meta (canonical, og:url, og:image, JSON-LD) to match your production domain.
